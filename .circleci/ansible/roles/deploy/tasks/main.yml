---
- name: Creates directory
  file:
    path: ~/var/www/html/app
    state: directory

- name: Copy index test page
  shell: /
    tar -xzvf artifact.tar.gz -C ~/var/www/html/app

- name: Executing node
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start
